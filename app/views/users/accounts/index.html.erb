<div class="uk-container uk-margin-top">
  <div uk-grid>
    <div class="uk-width-auto">
      <legend class="uk-legend">Users Accounts</legend>
    </div>
    <div class="uk-width-expand">
      <div class="uk-float-right">
        <%= link_to t(:new), new_users_account_path, class: 'uk-button uk-button-primary' %>
      </div>
    </div>
  </div>

  <table class="uk-table">
    <thead>
      <tr>
          <th class="uk-table-expand"><%=t(:email)%></th>
          <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @users_accounts.each do |users_account| %>
        <tr>
          <td><%= users_account.email %></td>
          <td class="uk-table-shrink"><%= link_to t(:show), users_account %></td>
          <td class="uk-table-shrink"><%= link_to t(:edit), edit_users_account_path(users_account) %></td>
          <td class="uk-table-shrink">
            <%= link_to t(:destroy), users_account, method: :delete, data: { confirm: t(:confirm) } %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>

<%= paginate @users_accounts, views_prefix: 'layouts' %>